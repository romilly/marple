# Statistics

Computing basic statistics in MARPLE: mean, variance, standard deviation, and correlation.

<!-- TODO: build with actual REPL output
     - mean: {(+/⍵)÷⍴⍵}
     - variance: {((+/⍵×⍵)÷⍴⍵)-((+/⍵)÷⍴⍵)*2}  or centered version
     - stddev: {(variance ⍵)*0.5}  using ∇ or named dependency
     - correlation: full worked example
     - apply to matrix columns using rank -->
