# Game of Life

Conway's Game of Life is a classic APL demonstration — the entire simulation fits in a few lines. This example builds it step by step.

<!-- TODO: build with actual REPL output
     - represent the board as a boolean matrix
     - count neighbours using outer-product shift or rotate approach
     - apply the rules with boolean logic
     - the complete life function as a dfn
     - running it with iteration
     Note: this is a well-known APL showcase; make sure the MARPLE version
     works correctly with flat arrays and the available primitives -->
