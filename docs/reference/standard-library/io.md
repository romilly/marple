# File I/O

File I/O in MARPLE uses system functions (quad-names), not the standard library namespace.

See [System Variables and Functions](../system/system-variables.md) for documentation of:

- `⎕NREAD` — read a text file
- `⎕NWRITE` — write a text file
- `⎕NEXISTS` — check if a file exists
- `⎕NDELETE` — delete a file
