# Standard Library (`$:`)

The standard library is always available via the `$:` prefix. Import functions for convenience with `#import`.

- [`$:io`](io.md) — file I/O
- [`$:str`](str.md) — string utilities
- [`$:sys`](sys.md) — system access
- [`$:error`](error.md) — error handling
