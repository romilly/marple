⎕VER
⍝ ═══════════════════════════════════════════════════
⍝ MARPLE Demo 10: System Functions
⍝ ═══════════════════════════════════════════════════

⍝ ── ⎕DL: Delay ──
⎕TS
⎕DL 0.5
⎕TS

⍝ ── ⎕NL: Name List ──
double←{⍵+⍵}
triple←{⍵+⍵+⍵}
⍝ List all functions (name class 3)
⎕NL 3

x←42
y←⍳5
⍝ List all variables (name class 2)
⎕NL 2

⍝ ── ⎕CSV: Import CSV data ──
⎕NREAD 'examples/scores.csv'
⎕CSV 'examples/scores.csv'
name
score
grade
+/score
⌈/score
