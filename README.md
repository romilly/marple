# marple

Mini APL in Python Language Experiment. Uses APL arrays (shape + flat data) as the internal data model.

## Installation

```bash
pip install -e .
```

## Development

```bash
pip install -e .[test]
pytest
```