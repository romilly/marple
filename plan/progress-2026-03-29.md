# Progress Report 2026-03-29

## What was done

### Test Recovery (185 tests converted)

Recovered 185 missing tests from git history (pre-deletion commit 3879a34^) and converted them to use the new `Interpreter` class. Tests went from 389 to 574 in new_engine, 552 to 737 overall.

### Feature Implementation (10 xfail tests fixed)

Implemented 6 missing features to fix all 10 xfail tests:
- **ClassError** on name class change (function→array, array→function)
- **⎕RL** re-seeds Python RNG for deterministic roll/deal
- **⎕FX** classifies dops (containing ⍺⍺/⍵⍵) as NC=4 operators
- **⎕FR** validation rejects values other than 645 or 1287
- **⎕CR** splits multi-line sources into matrix rows
- **⎕CSV** implementation: reads CSV files, creates column variables

### Hexagonal Architecture

- **Console port** — `Console` ABC (read_line, write, writeln) with `TerminalConsole` (real) and `FakeConsole` (test) adapters. REPL logic extracted into `run_repl(interp, console)`. Coverage 37% → 61%.
- **FileSystem port** — `FileSystem` ABC (8 methods) with `OsFileSystem` (real) and `FakeFileSystem` (in-memory) adapters. `Environment` holds the fs reference. executor, workspace, script all use the port.
- **Law of Demeter** — `Executor.fs` property delegates to `env.fs`

### Pyright — 0 errors

Fixed all 6 pre-existing pyright errors:
- repl.py: removed conditional workspace import
- namespace.py: standard typing import
- web/server.py: fixed FileResponse return type, standard typing import

### Power Operator (⍣)

- `(f⍣n) Y` iterates n times, negative → DOMAIN ERROR
- `(f⍣g) Y` converges when test function `g` returns 1
- Works with `≡`, `=`, custom convergence functions, left args
- 12 tests

### Tail Call Optimization

- `∇` self-calls in tail position run in constant stack space
- `_TailCall` sentinel returned from `_execute_body()`, caught by `apply()` which loops
- Countdown from 10000 and factorial(1000) complete without stack overflow
- 10 tests

### Rotate/Reverse Fixes

- **⌽ on matrices** — now reverses/rotates along last axis (was operating on flat data)
- **⊖ added** — first-axis reverse/rotate (monadic and dyadic)
- Both work for arrays of any rank (not just rank-2)
- 14 tests

### Conway's Game of Life

Built up step-by-step to a single self-contained dfn:
```apl
life←{⎕IO←0 ⋄ s←{(1↑⍺)⊖(1↓⍺)⌽⍵} ⋄ P←(⍉3 3⊤⍳9)-1 ⋄ N←(+⌿P(s⍤1 2)⍵)-⍵ ⋄ (N=3)∨⍵∧N=2}
```
- Uses rank operator to apply 9 shift pairs, +⌿ to sum neighbours
- Shift pairs generated programmatically with ⊤ and ⍳ (not hardcoded)
- Extended encode (⊤) to support vector right argument
- Localised system variables in dfns (⎕IO←0 inside dfn doesn't leak)
- Multi-line dfn support in script runner and pico_client
- `life` workspace with glider, shift, show, and life dfns
- PRIDE video recorded

### MicroPython Compatibility

- `pico_stubs/abc.py` and `pico_stubs/typing.py` — stub modules for MicroPython
- Deploy script strips `from __future__ import annotations`
- Clean imports in source (no try/except wrappers)
- `Environment.copy()` avoids `__new__`
- Tested: arithmetic, TCO, power operator, Life all run on Pico 2

### Pico/Presto Improvements

- Presto display scrolls instead of clearing when full
- Long lines truncated to fit 480px display
- pico_client replaces em dashes, box-drawing chars for Pico font
- pico_client `--script` flag for running .marple files
- Multi-line dfn accumulation in pico_client
- Deploy script updated for new engine architecture
- pico_eval.py updated to use Interpreter class

### PRIDE Web IDE

- Added ⍣ (Power) to language bar
- Added )SAVE, )LOAD, )LIB system commands
- Improved error message when system function used without argument

### Other

- Fixed crash when downcast encounters infinity
- Updated README for v0.5.2
- Version bumped to 0.5.2

### API Refactoring (v0.5.7)

- `format_result` moved from `repl.py` to `formatting.py` with rank-3+ display
- `Interpreter.execute()` returns `EvalResult(value, silent, display_text)`
- `system_commands.py` — shared dispatcher, eliminates duplication in REPL and web server
- `Environment.list_variables()` and `list_functions()` — no more magic NC constants
- `parser.is_balanced()` — brace-balance check for multi-line input

### Factorial and Binomial (v0.5.3)

- Monadic `!n` — factorial via `math.gamma(n+1)`
- Dyadic `k!n` — binomial coefficient
- Both pervade

### ⎕AI and ⎕TS Fix (v0.5.4)

- `⎕AI` returns 4-element vector: user ID, CPU ms, connect ms, keying time
- `⎕TS` fixed: reports 0 ms on MicroPython (honest, RTC doesn't track ms)

### Multi-axis Take/Drop (v0.5.5–0.5.6)

- `2 3↑M` takes 2 rows and 3 columns
- Character fill (space) for character arrays
- Take/drop/catenate fixed for matrices (were operating on flat data)

### Jupyter Kernel (v0.5.8)

- `src/marple/jupyter/` subpackage with kernel, HTML renderer, installer
- HTML table output for vectors, matrices, rank-3+ slices
- Tab completion from workspace names
- Shift+Tab introspection (shape, source, type)
- Multi-line dfn detection via `is_balanced()`
- System commands in cells via shared dispatcher
- Backtick-to-glyph translation via `expand_glyphs()`
- Language bar for classic Notebook via `kernel.js`
- Install: `pip install marple-lang[jupyter] && marple-jupyter-install`
- 19 tests (8 HTML render + 11 kernel)

## Stats

- **825 tests**, 1 skipped, 0 pyright errors
- Runs on CPython 3.10+, MicroPython (Pico 2), and Jupyter
- Version 0.5.8
