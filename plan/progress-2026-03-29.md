# Progress Report 2026-03-29

## Summary

Massive session: recovered 185 missing tests, implemented 6 new features, added hexagonal architecture (Console + FileSystem ports), built Conway's Game of Life, added Jupyter kernel, Bob Smith's partition functions, and fixed numerous bugs. Version 0.5.0 → 0.5.11.

## What was done

### Test Recovery (185 tests converted)

Recovered tests from git history (pre-deletion commit 3879a34^) and converted to new `Interpreter` class. Tests went from 389 to 574 in new_engine.

### Feature Implementation (10 xfail tests fixed)

Implemented 6 missing features: ClassError on name class change, ⎕RL re-seeding, ⎕FX dop classification (NC=4), ⎕FR validation, ⎕CR multi-line, ⎕CSV.

### Hexagonal Architecture

- **Console port** — `Console` ABC with `TerminalConsole` and `FakeConsole` adapters. REPL coverage 37% → 61%.
- **FileSystem port** — `FileSystem` ABC with `OsFileSystem` and `FakeFileSystem` adapters. `Environment` holds `fs` reference; `Executor.fs` property (Law of Demeter fix).
- Pyright: all 6 pre-existing errors fixed → 0 errors.

### Power Operator (⍣) — 12 tests

`(f⍣n)` iterates n times, `(f⍣≡)` converges to fixed point. Custom convergence functions. Left arg support.

### Tail Call Optimization — 10 tests

`∇` self-calls in tail position run in constant stack space. `_TailCall` sentinel in `dfn_binding.py`. Countdown from 10000 and factorial(1000) work.

### Rotate/Reverse Fixes — 14 tests

- `⌽` fixed for matrices (was operating on flat data)
- `⊖` added (first-axis rotate/reverse)
- Both work for any rank

### Conway's Game of Life — 9 tests

Single self-contained dfn using rank, reduce, and encode:
```apl
life←{⎕IO←0 ⋄ s←{(1↑⍺)⊖(1↓⍺)⌽⍵} ⋄ P←(⍉3 3⊤⍳9)-1 ⋄ N←(+⌿P(s⍤1 2)⍵)-⍵ ⋄ (N=3)∨⍵∧N=2}
```
- Shift pairs generated with ⊤ and ⍳ (encode extended to vector right arg)
- Localised system variables (⎕IO←0 inside dfn doesn't leak)
- `life` workspace with glider, shift, show dfns

### API Refactoring — 7 tests

- `format_result` moved to `formatting.py` with rank-3+ display
- `Interpreter.execute()` → `EvalResult(value, silent, display_text)`
- `system_commands.py` shared dispatcher (REPL, web, Jupyter)
- `Environment.list_variables()` / `list_functions()`
- `parser.is_balanced()` for multi-line input detection

### Jupyter Kernel — 19 tests

- `src/marple/jupyter/` subpackage: kernel, HTML renderer, installer
- HTML table output, tab completion, Shift+Tab introspection
- Multi-line dfns, system commands, backtick translation
- Install: `pip install marple-lang[jupyter] && marple-jupyter-install`

### Factorial and Binomial — 10 tests

Monadic `!n` (factorial via gamma), dyadic `k!n` (binomial coefficient). Both pervade.

### ⎕AI — 5 tests

Account information: user ID, CPU time (ms), connect time (ms), keying time. Works on CPython and MicroPython.

### Multi-axis Take/Drop — 9 tests

`2 3↑M` takes 2 rows and 3 cols. Character fill for character arrays. Take/drop/catenate fixed for matrices.

### Partition Functions Workspace — 12 tests

Bob Smith's partition techniques (STSC 1982): N∆, P∆ dops plus 10 partition dfns (pplred, pplscan, pnescan, porscan, pandscan, preverse, pnered, peqred, porred, pandred). DESCRIPTION and ⎕LX for self-documenting workspace.

### ⎕LX (Latent Expression) — 4 tests

System variable executed after workspace load. `)LOAD partfns` shows description automatically.

### Reduce on Empty Arrays — 10 tests

Identity elements: `+/⍳0` → 0, `×/⍳0` → 1, `⌈/⍳0` → -inf, etc. Comparison functions (=, ≠, <, ≤, >, ≥) added to operator resolve table.

### ∆ and ⍙ in Identifiers

Tokenizer now allows `∆` and `⍙` in identifier names (APL standard).

### MicroPython Compatibility

- `pico_stubs/abc.py` and `pico_stubs/typing.py` stub modules
- Deploy script strips `from __future__ import annotations`
- `Environment.copy()` avoids `__new__`
- Tested on Pico 2: arithmetic, TCO, power operator, Life

### Pico/Presto Improvements

- Presto display scrolls instead of clearing
- Long lines truncated to 480px
- pico_client: `--script` flag, multi-line dfn accumulation, Unicode char substitution
- Deploy script updated for new engine architecture

### PRIDE Web IDE

- ⍣ in language bar
- )SAVE, )LOAD, )LIB system commands
- Improved error message for system function without argument

### Infrastructure

- Fixed crash when downcast encounters infinity
- ⎕TS: honest 0ms on MicroPython
- System commands in script runner (`)LOAD` etc. work in .marple files)
- Demo video recorder: deletes WebM intermediates after MP4 conversion
- All 12 demo videos regenerated
- Platform notes: Linux, macOS, Windows
- Jupyter documentation (docs/how-to/jupyter.md)
- README, CHANGELOG, installation docs fully updated
- Published to PyPI: marple-lang 0.5.11

## Stats

- **851 tests**, 0 pyright errors
- Version 0.5.11
- Runs on CPython 3.10+, MicroPython (Pico 2), and Jupyter

## Known items for next session

- **Partition function example on Pico** — deploy updated code, run 13_partition.marple on Presto
- **More partition tests** — the document has application examples (Section 5) not yet tested: error detection, accumulating accounts with mixed types
- **⍪ (catenate first axis)** — needed for building matrices row by row; currently missing
- **Nested arrays** — the big missing feature for Scholes' Game of Life and many other idioms
- **Script runner multi-line dfn in pico_client** — verify `)LOAD` works on Pico
- **PRIDE demo recorder** — WebM cleanup still leaves .pride_videos/ directory occasionally
- **Jupyter startup time** — numpy import is slow; consider lazy import
- **⎕LX display** — currently only shown via `)LOAD` system command; should also work when loading workspace programmatically
