# Progress Report 2026-04-02

## Summary

Completed platform abstractions 5 (Timer) and 7 (Terminal), fixed the parser hang bug, unified the Pico eval loop to use the standard REPL, fixed ⎕DR type codes, and began preparation for the APLArray refactoring. Version 0.5.45 → 0.5.51.

## What was done

### Parser hang on unbalanced braces — fixed

- `_parse_dfn` now checks for EOF in its while loop
- Raises `SYNTAX ERROR: Unmatched {` instead of hanging
- Fast test suite dropped from ~10s to ~1.3s (old hang-detection test removed)

### PicoConsole uses standard REPL

- `PicoConsole.read_line()` now reads from stdin with hex decoding
- Sentinel (`\x00`) sent before each `read_line` except the first
- `pico_eval.py` reduced to just `run_repl(interp, console, banner=False)`
- `PrestoConsole` wrapper mirrors I/O to Presto LCD
- Fixed: host must send `\n` not `\r\n` (MicroPython treats `\r\n` as two lines)
- Fixed: sentinel sent for silent expressions (comments, assignments)
- Fixed: `sys.stdout.flush()` doesn't exist on MicroPython — use `print()` via `_println`

### pico_client.py and pico_demo.py rewritten

- Both now use `PicoConnection` instead of raw serial
- Eliminates duplicated protocol handling

### Platform abstraction 5: Timer — complete

- `Timer` ABC in `ports/timer.py`
- `DesktopTimer`: `time.time()`, `resource.getrusage()`, `os.getuid()`
- `PicoTimer`: `time.ticks_ms()`, no CPU time, uid=1000
- `FakeTimer` for testing
- Stored on `Environment`, accessible from both `Interpreter` and `DfnBinding`
- Removed `_START_TIME` module-level variable and `import time` from executor.py

### Platform abstraction 7: Terminal — complete

- `CharSource` ABC in `ports/char_source.py`
- `UnixCharSource`: `tty.setraw()`, `os.read()`, UTF-8 multi-byte handling
- `GlyphLineEditor`: platform-independent backtick→glyph translation
- `TerminalConsole` uses `GlyphLineEditor` + `UnixCharSource`
- 9 new tests for glyph translation and line editing
- Known issue: backtick conflicts with UK keyboard dead keys and Dyalog layout

### ⎕DR type codes — fixed and moved to backend

- `data_type_code()` function in `backend.py`
- Corrected codes: 81 (boolean, was 11), 320 (character, was 80), 643 (int64, was 645)
- Dyadic ⎕DR updated to accept new codes
- Scalar comparisons now return boolean (was returning integer)
- All six comparison functions verified as boolean for both scalars and vectors
- DR tests moved to `test_dr_backend.py` (backend-specific)
- 27 DR backend tests

### APLArray refactoring — preparation started

- Plan written: Phase 0 (fix ⎕DR), Phase 1 (convert tests), Phase 2 (factory), Phase 3 (migrate primitives)
- Test conversion approach: Red-Green cycle per test, check value + ⎕DR type
- Tests cannot assert exact numeric type codes — must test membership in sets
- Began converting `test_assignment.py`: added scalar, vector, and character assignment tests
- Decision: Python fallback without backend should not be supported

## Test counts

- **971 fast tests** in ~1.4s
- **8 slow tests** in ~1s
- **45 Pico E2E tests** in ~47s (1 skipped)
- Version 0.5.51

## Known items for next session

- **Continue test conversion** — 170 `APLArray` assertions remain across 30 files
- **⎕WA on Pico** — deploy and test with updated code
- **Backtick/UK keyboard conflict** — noted in memory, defer
- **Platform abstraction 6 (APLArray)** — blocked by test conversion
- **Deploy script** — needs `desktop_timer.py` and `pico_timer.py` added (done but verify)
