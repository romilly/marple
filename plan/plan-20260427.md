# New Plan

I'm giving up on Claude Code for this project. It's beyond its capacity.

Goal: build a version with all non-pico tests green, using a facade to create instances of numpy_array, with no calls to APLArray().

Steps:

- ~~Create a failing test for the new facade~~.
- ~~Add a facade as a port, with an abstract apl_array(shape, data, type) method~~.
- ~~Strengthen the test to look for an array~~
- Implement an adapter that creates numpy arrays.
- Refactor the sites that create APLArray instances to use the facade.